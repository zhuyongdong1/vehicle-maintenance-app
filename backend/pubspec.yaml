name: backend
description: 车辆维修管理API服务
version: 1.0.0

environment:
  sdk: ^3.9.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.2
  shelf_cors_headers: ^0.1.5
  mysql1: ^0.20.0
  http: ^1.2.2
  crypto: ^3.0.6
  shelf_multipart: ^2.0.1
  mime: ^2.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
